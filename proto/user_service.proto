syntax = "proto3";

package user_service;

// Servizio per la verifica degli utenti
service UserService {
  // Funzione chiamata dal Data Collector
  rpc VerifyUser (UserRequest) returns (UserResponse);
}

message UserRequest {
  string email = 1;
}

message UserResponse {
  bool exists = 1;
}